---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: portfolio
  labels:
    app: backend
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: backend
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: portfolio
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: backend
    spec:
      initContainers:
        # Wait for primary database and run migrations
        - name: wait-for-db
          image: postgres:16-alpine
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: portfolio-secrets
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: portfolio-secrets
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: portfolio-config
                  key: POSTGRES_DB
          command:
            - sh
            - -c
            - |
              echo "Waiting for postgres-primary to be ready..."
              until PGPASSWORD=$POSTGRES_PASSWORD pg_isready -h postgres-primary -U $POSTGRES_USER -d $POSTGRES_DB; do
                echo "Primary database not ready, waiting..."
                sleep 5
              done
              echo "Primary database is ready!"
              
              # Optional: Wait for replica (with timeout)
              echo "Checking if replica is available..."
              REPLICA_TIMEOUT=60
              COUNTER=0
              while [ $COUNTER -lt $REPLICA_TIMEOUT ]; do
                if PGPASSWORD=$POSTGRES_PASSWORD pg_isready -h postgres-replica -U $POSTGRES_USER 2>/dev/null; then
                  # Check if it's actually in recovery mode
                  IS_REPLICA=$(PGPASSWORD=$POSTGRES_PASSWORD psql -h postgres-replica -U $POSTGRES_USER -d postgres -tAc "SELECT pg_is_in_recovery();" 2>/dev/null || echo "f")
                  if [ "$IS_REPLICA" = "t" ]; then
                    echo "Replica is ready and in recovery mode!"
                    break
                  fi
                fi
                echo "Replica not ready yet ($COUNTER/$REPLICA_TIMEOUT)..."
                sleep 2
                COUNTER=$((COUNTER + 2))
              done
              
              if [ $COUNTER -ge $REPLICA_TIMEOUT ]; then
                echo "WARNING: Replica not available, backend will use primary for reads"
              fi
              
              echo "Database check complete!"
        
        # Run database migrations (always against primary)
        - name: migrate
          image: portfolio-backend:latest
          imagePullPolicy: Never  # Use locally imported image
          command:
            - sh
            - -c
            - |
              echo "Running Alembic migrations against primary..."
              alembic upgrade head
              echo "Migrations complete!"
          env:
            - name: ALEMBIC_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: portfolio-secrets
                  key: ALEMBIC_DATABASE_URL
          envFrom:
            - configMapRef:
                name: portfolio-config
            - secretRef:
                name: portfolio-secrets
      containers:
        - name: backend
          image: portfolio-backend:latest
          imagePullPolicy: Never  # Use locally imported image
          ports:
            - containerPort: 8000
              name: http
          command:
            - uvicorn
            - app.main:app
            - --host
            - "0.0.0.0"
            - --port
            - "8000"
            - --workers
            - "2"
          env:
            # Explicitly set database URLs for read/write splitting
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: portfolio-secrets
                  key: DATABASE_URL
            - name: DATABASE_URL_PRIMARY
              valueFrom:
                secretKeyRef:
                  name: portfolio-secrets
                  key: DATABASE_URL_PRIMARY
            - name: DATABASE_URL_REPLICA
              valueFrom:
                secretKeyRef:
                  name: portfolio-secrets
                  key: DATABASE_URL_REPLICA
            - name: ALEMBIC_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: portfolio-secrets
                  key: ALEMBIC_DATABASE_URL
          envFrom:
            - configMapRef:
                name: portfolio-config
            - secretRef:
                name: portfolio-secrets
          livenessProbe:
            httpGet:
              path: /api/v1/health/
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /api/v1/health/
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 2
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "2Gi"
              cpu: "1000m"
# =============================================================================
# 05-postgres-replica.yaml
# PostgreSQL 16 Replica — StatefulSet + ClusterIP Service
#
# Uses the official postgres:16 image (not the custom portfolio-postgres:16).
# The replica needs no pg_cron, no init scripts, no custom extensions.
# imagePullPolicy: IfNotPresent — pulled from Docker Hub on first use,
# cached on the node after that.
#
# =============================================================================
# RESOURCES IN THIS FILE
# =============================================================================
#
# 1. Service (ClusterIP)
#    Stable internal DNS name: postgres-replica:5432
#    The backend read pool connects here for all SELECT queries.
#    On startup the backend calls pg_is_in_recovery() to verify the replica
#    is actually in standby mode before routing reads to it.
#
# 2. StatefulSet
#    StatefulSet for the same reasons as the primary (04-postgres-primary.yaml):
#    stable pod name, stable PVC reattachment, ordered startup.
#    The replica always starts after the primary because the replica-init
#    init container blocks until postgres-primary:5432 accepts connections.
#
# =============================================================================
# VOLUME ARCHITECTURE
# =============================================================================
#
#   Volume Name       Type        Backed By                         Survives Restart
#   ──────────────    ──────────  ──────────────────────────────    ────────────────
#   postgres-data     PVC (RWO)   /var/lib/rancher/k3s/storage/     YES (node disk)
#   postgres-certs    emptyDir    node tmpfs                         NO (regenerated)
#
#   postgres-data
#     Bound to postgres-replica-data (declared in 03-pvcs.yaml).
#     On first boot replica-init clones the primary into this PVC via
#     pg_basebackup. On subsequent restarts the data already exists and
#     replica-init exits immediately (standby.signal present).
#
#   ReadWriteOnce (RWO): only one pod per node can mount this PVC at a time.
#   StatefulSet handles this correctly — same pod always gets the same PVC.
#
#   postgres-certs
#     emptyDir written by ssl-certgen, read-only by the main container.
#     Regenerated on every pod restart.
#     https://kubernetes.io/docs/concepts/storage/volumes/ (search: emptyDir)
#
# =============================================================================
# BOOT LIFECYCLE
# =============================================================================
#
# 1. KUBERNETES SCHEDULES THE POD
#    Both volumes are mounted before any container runs. Neither is created
#    here — they are declared:
#      /var/lib/postgresql/data  <- postgres-data PVC (exists from 03-pvcs.yaml)
#      /var/lib/postgresql/certs <- postgres-certs emptyDir (always starts empty)
#
# 2. INIT CONTAINER: ssl-certgen (alpine:3.20)
#    Generates a self-signed cert and writes it into postgres-certs emptyDir.
#    chowns to UID 999 (postgres OS user) so the main container can read it.
#    Short-lived: openssl runs and exits, 32Mi RAM is generous for this.
#
# 3. INIT CONTAINER: replica-init (postgres:16)
#    The core of this file. Has one job: make PGDATA a valid streaming replica.
#    Runs on every pod start but exits immediately if already initialized.
#    Full explanation of each step in the REPLICA-INIT SCRIPT section below.
#
# 4. MAIN CONTAINER STARTS (postgres:16)
#    PGDATA is already a valid replica clone (written by replica-init).
#    standby.signal tells PostgreSQL to enter recovery mode — reads only,
#    WAL applied continuously from the primary via streaming replication.
#    hot_standby=on (set in postgresql.auto.conf by replica-init) allows
#    SELECT queries to run while in recovery mode.
#
# =============================================================================
# REPLICA-INIT SCRIPT — what each step does and why
# =============================================================================
#
# AUTH SETUP
#   Sets both PGPASSWORD and .pgpass so no postgres tool ever prompts
#   for a password interactively. An interactive prompt would hang the
#   init container forever since there is no human here to type anything.
#
#     export PGPASSWORD="$POSTGRES_PASSWORD"
#     echo "postgres-primary:5432:*:$POSTGRES_USER:$POSTGRES_PASSWORD" > /tmp/.pgpass
#     chmod 600 /tmp/.pgpass
#     export PGPASSFILE=/tmp/.pgpass
#
# REBOOT GUARD
#   Checks if PGDATA was already initialized in a previous run:
#     PG_VERSION missing        -> first run, proceed to clone
#     standby.signal present    -> healthy replica, exit 0, nothing to do
#     PG_VERSION but no signal  -> previous run failed midway, wipe and redo
#
#   hub.docker.com/_/postgres (section: "How to extend this image") and
#   github.com/docker-library/postgres/blob/master/docker-entrypoint.sh
#   document the PG_VERSION check used by the official entrypoint — same
#   file, same source of truth.
#
#     if [ -f "$PGDATA/PG_VERSION" ]; then
#       [ -f "$PGDATA/standby.signal" ] && exit 0
#       rm -rf "$PGDATA"/*
#     fi
#
# WAIT FOR PRIMARY
#   Polls postgres-primary (Kubernetes Service DNS) until it accepts connections.
#   The Service only routes traffic once the primary's readiness probe passes,
#   so this loop guarantees the primary is genuinely ready before cloning starts.
#
#     until pg_isready -h postgres-primary -U "$POSTGRES_USER"; do
#       sleep 5
#     done
#
# REPLICATION SLOT
#   Creates replica_slot_1 on the primary if it does not already exist.
#   The slot is the primary's promise to hold WAL until this replica consumes it.
#   Idempotent: WHERE NOT EXISTS guard makes it safe to run on every pod start.
#   || true prevents set -e from killing the script if the slot already exists
#   (race condition if two replica pods somehow ran simultaneously).
#
#     psql -h postgres-primary -U "$POSTGRES_USER" -d postgres -c \
#       "SELECT pg_create_physical_replication_slot('replica_slot_1')
#        WHERE NOT EXISTS (
#          SELECT 1 FROM pg_replication_slots WHERE slot_name='replica_slot_1'
#        );" || true
#
# BASE BACKUP
#   pg_basebackup copies the entire primary database into PGDATA.
#   This is the most memory-intensive moment in the replica lifecycle —
#   it buffers WAL stream + file writes simultaneously (128Mi/256Mi limits).
#
#   Flags:
#     -Fp               plain format (not tar), files written directly to PGDATA
#     -Xs               stream WAL in parallel during the copy so no changes
#                       are missed between backup start and finish
#     -R                writes primary_conninfo into postgresql.auto.conf and
#                       creates standby.signal automatically
#     -P                shows progress
#     --checkpoint=fast forces an immediate checkpoint on the primary so the
#                       backup starts from a clean state rather than waiting
#                       for the next scheduled checkpoint
#     --wal-method=stream same as -Xs, explicit for clarity
#
#     pg_basebackup \
#       -h postgres-primary \
#       -U "$POSTGRES_USER" \
#       -D "$PGDATA" \
#       -Fp -Xs -R -P \
#       --checkpoint=fast \
#       --wal-method=stream
#
# PG_HBA OVERWRITE
#   pg_basebackup copied the primary's pg_hba.conf. We overwrite it with
#   replica-specific rules. cat > overwrites, not appends.
#   The primary's pg_hba.conf is managed via a symlink to pg_hba.ssl.conf
#   (written by 01-init-ssl.sh) — that symlink does not survive pg_basebackup,
#   so we write a flat file here instead.
#   chown postgres:postgres (UID 999) required — postgres refuses to start
#   if config files are not owned by its own user.
#   chmod 600 — no group or world access.
#
#     cat > "$PGDATA/pg_hba.conf" <<EOF
#     local   all   all                         scram-sha-256
#     host    all   all   127.0.0.1/32          scram-sha-256
#     host    all   all   ::1/128               scram-sha-256
#     hostssl all   all   0.0.0.0/0             scram-sha-256
#     hostssl all   all   ::/0                  scram-sha-256
#     hostssl replication all 0.0.0.0/0         scram-sha-256
#     hostssl replication all ::/0              scram-sha-256
#     EOF
#     chown postgres:postgres "$PGDATA/pg_hba.conf"
#     chmod 600 "$PGDATA/pg_hba.conf"
#
# POSTGRESQL.AUTO.CONF
#   cat >> appends to what -R already wrote. Sets replica-specific settings:
#
#     hot_standby          allows the replica to serve reads while in recovery
#                          mode. without this the replica rejects all queries
#                          during WAL replay — it would be useless for read traffic.
#     primary_conninfo     how to connect to the primary to stream WAL.
#                          uses postgres-primary Kubernetes Service DNS, SSL required.
#     primary_slot_name    which replication slot to use. the slot holds WAL
#                          on the primary until this replica confirms consumption.
#     recovery_target_timeline  follow the latest timeline. relevant if the primary
#                          ever fails over — the replica will follow the new timeline
#                          rather than getting stuck on the old one.
#
#     cat >> "$PGDATA/postgresql.auto.conf" <<EOF
#     hot_standby = on
#     primary_conninfo = 'host=postgres-primary port=5432 user=... sslmode=require application_name=replica1'
#     primary_slot_name = 'replica_slot_1'
#     recovery_target_timeline = 'latest'
#     EOF
#
# STANDBY SIGNAL + PERMISSIONS
#   standby.signal tells postgres to enter recovery mode on startup.
#   Without this file postgres sees a full initialized PGDATA and boots
#   as a standalone primary — it would start accepting writes and silently
#   break replication with no error.
#   chmod 700 on PGDATA — directories need the execute bit to be traversable.
#   chmod 600 would prevent even the postgres process from entering its own
#   data directory.
#
#     touch "$PGDATA/standby.signal"
#     chmod 700 "$PGDATA"
#
# =============================================================================

---
apiVersion: v1
kind: Service
metadata:
  name: postgres-replica
  namespace: portfolio
  labels:
    app: postgres-replica
    role: replica
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  selector:
    app: postgres-replica

---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-replica
  namespace: portfolio
spec:
  serviceName: postgres-replica
  replicas: 1
  selector:
    matchLabels:
      app: postgres-replica
  template:
    metadata:
      labels:
        app: postgres-replica
        role: replica
    spec:
      initContainers:
        - name: ssl-certgen
          image: alpine:3.20
          command: ["sh","-c"]
          args:
            - |
              apk add --no-cache openssl
              mkdir -p /certs
              openssl req -new -x509 -nodes -days 3650 \
                -subj "/C=CL/ST=NA/L=NA/O=portfolio/CN=postgres-replica" \
                -out /certs/server.crt \
                -keyout /certs/server.key
              chmod 600 /certs/server.key
              chmod 644 /certs/server.crt
              chown 999:999 /certs/server.*
          volumeMounts:
            - name: postgres-certs
              mountPath: /certs
          resources:
            requests:
              memory: "32Mi"
              cpu: "50m"
            limits:
              memory: "64Mi"
              cpu: "100m"

        - name: replica-init
          image: postgres:16
          imagePullPolicy: IfNotPresent
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: portfolio-secrets
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: portfolio-secrets
                  key: POSTGRES_PASSWORD
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          command: ["sh","-c"]
          args:
            - |
              set -e

              export PGPASSWORD="$POSTGRES_PASSWORD"
              echo "postgres-primary:5432:*:$POSTGRES_USER:$POSTGRES_PASSWORD" > /tmp/.pgpass
              chmod 600 /tmp/.pgpass
              export PGPASSFILE=/tmp/.pgpass

              if [ -f "$PGDATA/PG_VERSION" ]; then
                [ -f "$PGDATA/standby.signal" ] && exit 0
                rm -rf "$PGDATA"/*
              fi

              until pg_isready -h postgres-primary -U "$POSTGRES_USER"; do
                sleep 5
              done

              psql -h postgres-primary -U "$POSTGRES_USER" -d postgres -c \
                "SELECT pg_create_physical_replication_slot('replica_slot_1')
                 WHERE NOT EXISTS (
                   SELECT 1 FROM pg_replication_slots WHERE slot_name='replica_slot_1'
                 );" || true

              pg_basebackup \
                -h postgres-primary \
                -U "$POSTGRES_USER" \
                -D "$PGDATA" \
                -Fp -Xs -R -P \
                --checkpoint=fast \
                --wal-method=stream

              cat > "$PGDATA/pg_hba.conf" <<EOF
              local   all   all                         scram-sha-256
              host    all   all   127.0.0.1/32          scram-sha-256
              host    all   all   ::1/128               scram-sha-256
              hostssl all   all   0.0.0.0/0             scram-sha-256
              hostssl all   all   ::/0                  scram-sha-256
              hostssl replication all 0.0.0.0/0         scram-sha-256
              hostssl replication all ::/0              scram-sha-256
              EOF

              chown postgres:postgres "$PGDATA/pg_hba.conf"
              chmod 600 "$PGDATA/pg_hba.conf"

              cat >> "$PGDATA/postgresql.auto.conf" <<EOF
              hot_standby = on
              primary_conninfo = 'host=postgres-primary port=5432 user=$POSTGRES_USER password=$POSTGRES_PASSWORD sslmode=require application_name=replica1'
              primary_slot_name = 'replica_slot_1'
              recovery_target_timeline = 'latest'
              EOF

              touch "$PGDATA/standby.signal"
              chmod 700 "$PGDATA"

          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data  # node disk: /var/lib/rancher/k3s/storage/pvc-<uid>_portfolio_postgres-replica-data/
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "300m"

      containers:
        - name: postgres
          image: postgres:16
          imagePullPolicy: IfNotPresent
          env:
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          args:
            - postgres
            - -c
            - ssl=on
            - -c
            - ssl_cert_file=/var/lib/postgresql/certs/server.crt
            - -c
            - ssl_key_file=/var/lib/postgresql/certs/server.key
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data  # node disk: /var/lib/rancher/k3s/storage/pvc-<uid>_portfolio_postgres-replica-data/
            - name: postgres-certs
              mountPath: /var/lib/postgresql/certs  # node tmpfs: emptyDir, not a PVC, no path under k3s/storage/
              readOnly: true
          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - pg_isready -U postgres
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 5
          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - pg_isready -U postgres
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
          resources:
            # Replica is read-only: no writes, no WAL generation, no pg_cron.
            # Less memory pressure than primary — 128Mi/256Mi is appropriate.
            # Primary uses 192Mi/384Mi because it handles writes + replication overhead.
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"

      volumes:
        - name: postgres-data
          persistentVolumeClaim:
            claimName: postgres-replica-data
        - name: postgres-certs
          emptyDir: {}